define(['bootstrap'], function (bs) {

    var Sound = function () {

        this.init = function () {
            // start background noise
            // listen for death event and play death noise, etc
        };
    };

    return new Sound();
});