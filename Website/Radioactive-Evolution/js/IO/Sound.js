define(['module/controller/pubsub'], function (pubsub) {

    var Sound = function () {

        this.init = function () {
            // start background noise
            // listen for death event and play death noise, etc
        };
    };

    return new Sound();
});